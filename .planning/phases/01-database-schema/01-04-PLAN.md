---
phase: 01-database-schema
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/lib/types/database.types.ts
autonomous: true
requirements: ["DB-01", "DB-02", "DB-03", "DB-04", "DB-05", "DB-06", "DB-07"]
must_haves:
  truths:
    - "src/lib/types/database.types.ts существует и не пустой"
    - "Файл содержит типы для всех 15 таблиц"
    - "npm run build или npx tsc --noEmit завершается без ошибок"
  artifacts:
    - src/lib/types/database.types.ts
  key_links:
    - "database.types.ts будет импортироваться в src/lib/supabase/client.ts и server.ts"
---

<objective>
Сгенерировать TypeScript типы из Supabase схемы и проверить что проект собирается.

Purpose: Типобезопасность для всех взаимодействий с БД. Без этого нельзя начинать Phase 2 (аутентификация).
Output: src/lib/types/database.types.ts с типами для всех 15 таблиц.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Инициализация Next.js проекта (если ещё не создан)</name>
  <files>package.json, tsconfig.json</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Проверить существует ли package.json. Если нет — создать Next.js 14 проект:

```bash
# Проверка
[ -f package.json ] && echo "exists" || echo "missing"
```

Если package.json отсутствует:
```bash
npx create-next-app@14 . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
```

Если проект уже существует — пропустить этот шаг.

После инициализации проверить что tsconfig.json существует.
  </action>
  <verify>[ -f package.json ] && echo "OK" || echo "MISSING"</verify>
  <acceptance_criteria>
    - "package.json существует"
    - "tsconfig.json существует"
  </acceptance_criteria>
  <done>Next.js проект инициализирован</done>
</task>

<task type="auto">
  <name>Task 2: Создание структуры директорий lib</name>
  <files>src/lib/types/.gitkeep</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Создать директорию src/lib/types/ если не существует:

```bash
mkdir -p src/lib/types
mkdir -p src/lib/supabase
mkdir -p src/lib/actions
```
  </action>
  <verify>[ -d src/lib/types ] && echo "OK"</verify>
  <acceptance_criteria>
    - "Директория src/lib/types/ существует"
    - "Директория src/lib/supabase/ существует"
    - "Директория src/lib/actions/ существует"
  </acceptance_criteria>
  <done>Структура директорий создана</done>
</task>

<task type="auto">
  <name>Task 3: Генерация TypeScript типов из Supabase</name>
  <files>src/lib/types/database.types.ts</files>
  <read_first></read_first>
  <action>
Сгенерировать типы из Supabase Cloud проекта:

```bash
supabase gen types typescript --linked > src/lib/types/database.types.ts
```

Если `--linked` не работает (проект не залинкован), использовать PROJECT_REF из .env.local:
```bash
# Найти project_ref из NEXT_PUBLIC_SUPABASE_URL
# URL формата: https://YOUR_REF.supabase.co
supabase gen types typescript --project-id YOUR_REF > src/lib/types/database.types.ts
```

Проверить что файл содержит интерфейсы для всех ключевых таблиц:
```bash
grep -c "profiles\|groups\|messages\|direct_messages" src/lib/types/database.types.ts
```
  </action>
  <verify>wc -l src/lib/types/database.types.ts</verify>
  <acceptance_criteria>
    - "src/lib/types/database.types.ts существует"
    - "Файл содержит 'profiles'"
    - "Файл содержит 'messages'"
    - "Файл содержит 'direct_messages'"
    - "Файл содержит 'message_attachments'"
    - "Файл не пустой (> 50 строк)"
  </acceptance_criteria>
  <done>database.types.ts сгенерирован и содержит все таблицы</done>
</task>

<task type="auto">
  <name>Task 4: Финальная проверка — сборка проекта</name>
  <files></files>
  <read_first>src/lib/types/database.types.ts</read_first>
  <action>
Запустить проверку TypeScript без полной сборки:

```bash
npx tsc --noEmit 2>&1 | head -20
```

Если есть ошибки — исправить. Чаще всего ошибки связаны с отсутствующими env variables или импортами.

Если проект пустой (только что создан через create-next-app) — ошибок быть не должно.
  </action>
  <verify>npx tsc --noEmit 2>&1; echo "Exit: $?"</verify>
  <acceptance_criteria>
    - "npx tsc --noEmit завершается с exit code 0"
  </acceptance_criteria>
  <done>Проект собирается без TypeScript ошибок. Phase 1 завершена.</done>
</task>

</tasks>
