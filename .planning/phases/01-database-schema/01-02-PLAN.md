---
phase: 01-database-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - supabase/migrations/00004_rls_policies.sql
autonomous: true
requirements: ["DB-08"]
must_haves:
  truths:
    - "Все 15 таблиц имеют ALTER TABLE ENABLE ROW LEVEL SECURITY"
    - "Вспомогательные функции is_group_member, is_direct_chat_member, is_admin определены"
    - "Каждая таблица имеет хотя бы одну SELECT-политику"
    - "Политики папок: только владелец (auth.uid() = user_id)"
    - "Политики групп: только admin может создавать/удалять"
  artifacts:
    - supabase/migrations/00004_rls_policies.sql
  key_links:
    - "is_group_member() используется в политиках messages, message_reads, message_reactions, message_attachments, pinned_messages"
    - "is_admin() используется в политиках groups, group_members"
---

<objective>
Создать миграцию с RLS-политиками для всех 15 таблиц IntraChat.

Purpose: Безопасность — без RLS любой аутентифицированный пользователь может читать чужие сообщения и папки.
Output: supabase/migrations/00004_rls_policies.sql с ENABLE RLS + политиками для всех таблиц.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@supabase/migrations/00001_core_users_groups.sql
@supabase/migrations/00002_messages.sql
@supabase/migrations/00003_direct_messages.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция 4 — RLS для всех таблиц</name>
  <files>supabase/migrations/00004_rls_policies.sql</files>
  <read_first>supabase/migrations/00001_core_users_groups.sql, supabase/migrations/00002_messages.sql, supabase/migrations/00003_direct_messages.sql</read_first>
  <action>
Создать файл supabase/migrations/00004_rls_policies.sql:

```sql
-- 00004_rls_policies.sql
-- RLS: включение + политики для всех 15 таблиц

-- ============================================================
-- Вспомогательные функции (SECURITY DEFINER для обхода RLS)
-- ============================================================

CREATE OR REPLACE FUNCTION is_group_member(p_group_id uuid)
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
STABLE
AS $$
  SELECT EXISTS (
    SELECT 1 FROM group_members
    WHERE group_id = p_group_id AND user_id = auth.uid()
  );
$$;

CREATE OR REPLACE FUNCTION is_direct_chat_member(p_chat_id uuid)
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
STABLE
AS $$
  SELECT EXISTS (
    SELECT 1 FROM direct_chat_members
    WHERE chat_id = p_chat_id AND user_id = auth.uid()
  );
$$;

CREATE OR REPLACE FUNCTION is_admin()
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
STABLE
AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin' AND is_active = true
  );
$$;

-- ============================================================
-- ENABLE RLS на всех таблицах
-- ============================================================

ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE groups ENABLE ROW LEVEL SECURITY;
ALTER TABLE group_members ENABLE ROW LEVEL SECURITY;
ALTER TABLE folders ENABLE ROW LEVEL SECURITY;
ALTER TABLE folder_items ENABLE ROW LEVEL SECURITY;
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE message_reads ENABLE ROW LEVEL SECURITY;
ALTER TABLE message_reactions ENABLE ROW LEVEL SECURITY;
ALTER TABLE message_attachments ENABLE ROW LEVEL SECURITY;
ALTER TABLE pinned_messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE direct_chats ENABLE ROW LEVEL SECURITY;
ALTER TABLE direct_chat_members ENABLE ROW LEVEL SECURITY;
ALTER TABLE direct_messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE direct_message_reads ENABLE ROW LEVEL SECURITY;
ALTER TABLE direct_message_attachments ENABLE ROW LEVEL SECURITY;

-- ============================================================
-- profiles
-- ============================================================

CREATE POLICY "profiles_select" ON profiles
  FOR SELECT TO authenticated
  USING (auth.uid() IS NOT NULL AND is_active = true);

CREATE POLICY "profiles_insert" ON profiles
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = id);

CREATE POLICY "profiles_update" ON profiles
  FOR UPDATE TO authenticated
  USING (auth.uid() = id)
  WITH CHECK (auth.uid() = id);

-- ============================================================
-- groups
-- ============================================================

CREATE POLICY "groups_select" ON groups
  FOR SELECT TO authenticated
  USING (is_group_member(id) OR is_admin());

CREATE POLICY "groups_insert" ON groups
  FOR INSERT TO authenticated
  WITH CHECK (is_admin());

CREATE POLICY "groups_update" ON groups
  FOR UPDATE TO authenticated
  USING (is_admin());

CREATE POLICY "groups_delete" ON groups
  FOR DELETE TO authenticated
  USING (is_admin());

-- ============================================================
-- group_members
-- ============================================================

CREATE POLICY "group_members_select" ON group_members
  FOR SELECT TO authenticated
  USING (is_group_member(group_id) OR is_admin());

CREATE POLICY "group_members_insert" ON group_members
  FOR INSERT TO authenticated
  WITH CHECK (is_admin());

CREATE POLICY "group_members_delete" ON group_members
  FOR DELETE TO authenticated
  USING (is_admin());

-- ============================================================
-- folders (только владелец)
-- ============================================================

CREATE POLICY "folders_all" ON folders
  FOR ALL TO authenticated
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

-- ============================================================
-- folder_items
-- ============================================================

CREATE POLICY "folder_items_all" ON folder_items
  FOR ALL TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM folders
      WHERE folders.id = folder_items.folder_id
        AND folders.user_id = auth.uid()
    )
  )
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM folders
      WHERE folders.id = folder_items.folder_id
        AND folders.user_id = auth.uid()
    )
  );

-- ============================================================
-- messages
-- ============================================================

CREATE POLICY "messages_select" ON messages
  FOR SELECT TO authenticated
  USING (is_group_member(group_id));

CREATE POLICY "messages_insert" ON messages
  FOR INSERT TO authenticated
  WITH CHECK (is_group_member(group_id) AND auth.uid() = sender_id);

CREATE POLICY "messages_update" ON messages
  FOR UPDATE TO authenticated
  USING (auth.uid() = sender_id)
  WITH CHECK (auth.uid() = sender_id);

-- ============================================================
-- message_reads
-- ============================================================

CREATE POLICY "message_reads_select" ON message_reads
  FOR SELECT TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM messages m
      WHERE m.id = message_reads.message_id
        AND is_group_member(m.group_id)
    )
  );

CREATE POLICY "message_reads_insert" ON message_reads
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = user_id);

-- ============================================================
-- message_reactions
-- ============================================================

CREATE POLICY "message_reactions_select" ON message_reactions
  FOR SELECT TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM messages m
      WHERE m.id = message_reactions.message_id
        AND is_group_member(m.group_id)
    )
  );

CREATE POLICY "message_reactions_insert" ON message_reactions
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "message_reactions_delete" ON message_reactions
  FOR DELETE TO authenticated
  USING (auth.uid() = user_id);

-- ============================================================
-- message_attachments
-- ============================================================

CREATE POLICY "message_attachments_select" ON message_attachments
  FOR SELECT TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM messages m
      WHERE m.id = message_attachments.message_id
        AND is_group_member(m.group_id)
    )
  );

CREATE POLICY "message_attachments_insert" ON message_attachments
  FOR INSERT TO authenticated
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM messages m
      WHERE m.id = message_attachments.message_id
        AND is_group_member(m.group_id)
        AND m.sender_id = auth.uid()
    )
  );

-- ============================================================
-- pinned_messages
-- ============================================================

CREATE POLICY "pinned_messages_select" ON pinned_messages
  FOR SELECT TO authenticated
  USING (is_group_member(group_id));

CREATE POLICY "pinned_messages_insert" ON pinned_messages
  FOR INSERT TO authenticated
  WITH CHECK (is_group_member(group_id));

CREATE POLICY "pinned_messages_delete" ON pinned_messages
  FOR DELETE TO authenticated
  USING (is_group_member(group_id));

-- ============================================================
-- direct_chats
-- ============================================================

CREATE POLICY "direct_chats_select" ON direct_chats
  FOR SELECT TO authenticated
  USING (is_direct_chat_member(id));

CREATE POLICY "direct_chats_insert" ON direct_chats
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() IS NOT NULL);

-- ============================================================
-- direct_chat_members
-- ============================================================

CREATE POLICY "direct_chat_members_select" ON direct_chat_members
  FOR SELECT TO authenticated
  USING (is_direct_chat_member(chat_id));

CREATE POLICY "direct_chat_members_insert" ON direct_chat_members
  FOR INSERT TO authenticated
  WITH CHECK (
    auth.uid() = user_id
    OR is_direct_chat_member(chat_id)
  );

-- ============================================================
-- direct_messages
-- ============================================================

CREATE POLICY "direct_messages_select" ON direct_messages
  FOR SELECT TO authenticated
  USING (is_direct_chat_member(chat_id));

CREATE POLICY "direct_messages_insert" ON direct_messages
  FOR INSERT TO authenticated
  WITH CHECK (is_direct_chat_member(chat_id) AND auth.uid() = sender_id);

CREATE POLICY "direct_messages_update" ON direct_messages
  FOR UPDATE TO authenticated
  USING (auth.uid() = sender_id)
  WITH CHECK (auth.uid() = sender_id);

-- ============================================================
-- direct_message_reads
-- ============================================================

CREATE POLICY "direct_message_reads_select" ON direct_message_reads
  FOR SELECT TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM direct_messages dm
      WHERE dm.id = direct_message_reads.message_id
        AND is_direct_chat_member(dm.chat_id)
    )
  );

CREATE POLICY "direct_message_reads_insert" ON direct_message_reads
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = user_id);

-- ============================================================
-- direct_message_attachments
-- ============================================================

CREATE POLICY "direct_message_attachments_select" ON direct_message_attachments
  FOR SELECT TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM direct_messages dm
      WHERE dm.id = direct_message_attachments.message_id
        AND is_direct_chat_member(dm.chat_id)
    )
  );

CREATE POLICY "direct_message_attachments_insert" ON direct_message_attachments
  FOR INSERT TO authenticated
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM direct_messages dm
      WHERE dm.id = direct_message_attachments.message_id
        AND is_direct_chat_member(dm.chat_id)
        AND dm.sender_id = auth.uid()
    )
  );
```
  </action>
  <verify>grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/00004_rls_policies.sql</verify>
  <acceptance_criteria>
    - "supabase/migrations/00004_rls_policies.sql существует"
    - "grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/00004_rls_policies.sql возвращает 15"
    - "Файл содержит функции is_group_member, is_direct_chat_member, is_admin"
    - "Файл содержит 'SECURITY DEFINER'"
  </acceptance_criteria>
  <done>RLS включён на всех 15 таблицах, политики определены</done>
</task>

</tasks>
