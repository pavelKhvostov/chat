---
phase: 02-authentication
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - .env.local
autonomous: false
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04", "AUTH-05"]
user_setup:
  - "Скопировать ANON_KEY и SERVICE_ROLE_KEY из Supabase Dashboard → Settings → API в .env.local"
  - "Создать тестового пользователя в Supabase Dashboard → Authentication → Users"
must_haves:
  truths:
    - ".env.local содержит все три переменные окружения с реальными значениями"
    - "npm run dev запускается без ошибок"
    - "Страница /login рендерится в браузере"
    - "Вход с тестовым аккаунтом перенаправляет на /"
    - "Обновление страницы не разлогинивает"
  artifacts:
    - .env.local
  key_links:
    - ".env.local нужен для работы всего проекта"
---

<objective>
Заполнить .env.local реальными ключами Supabase и провести ручную проверку входа.

Purpose: Без реальных ключей приложение не может подключиться к Supabase. Финальная проверка Phase 2.
Output: Рабочая аутентификация — пользователь может войти и выйти.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить .env.local с URL проекта</name>
  <files>.env.local</files>
  <read_first>.env.local</read_first>
  <action>
Убедиться что .env.local содержит NEXT_PUBLIC_SUPABASE_URL. Если файл не содержит URL — добавить:

Supabase project URL для проекта cuuoyhikjawiyzgbrnku:
```
NEXT_PUBLIC_SUPABASE_URL=https://cuuoyhikjawiyzgbrnku.supabase.co
```

ВАЖНО: НЕ добавлять .env.local в git — убедиться что он в .gitignore.

Проверить .gitignore:
```bash
grep ".env" .gitignore || echo ".env*.local" >> .gitignore
```
  </action>
  <verify>grep "NEXT_PUBLIC_SUPABASE_URL" .env.local</verify>
  <acceptance_criteria>
    - ".env.local содержит NEXT_PUBLIC_SUPABASE_URL=https://cuuoyhikjawiyzgbrnku.supabase.co"
    - ".env.local есть в .gitignore"
  </acceptance_criteria>
  <done>URL проекта добавлен в .env.local</done>
</task>

<task type="checkpoint:manual">
  <name>Task 2: [CHECKPOINT] Добавить API ключи в .env.local</name>
  <files>.env.local</files>
  <read_first></read_first>
  <action>
ИНСТРУКЦИЯ ДЛЯ ПОЛЬЗОВАТЕЛЯ:

Откройте Supabase Dashboard → Settings → API:
https://supabase.com/dashboard/project/cuuoyhikjawiyzgbrnku/settings/api

Скопируйте значения в .env.local:

```
NEXT_PUBLIC_SUPABASE_URL=https://cuuoyhikjawiyzgbrnku.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon public key>
SUPABASE_SERVICE_ROLE_KEY=<service_role secret key>
```

После заполнения запустите:
```bash
npm run dev
```

Откройте http://localhost:3000 — должен быть редирект на /login.
  </action>
  <verify>echo "Manual checkpoint — user fills API keys"</verify>
  <acceptance_criteria>
    - ".env.local содержит NEXT_PUBLIC_SUPABASE_ANON_KEY (не пустой)"
    - ".env.local содержит SUPABASE_SERVICE_ROLE_KEY (не пустой)"
    - "npm run dev запускается без ошибок"
    - "http://localhost:3000 редиректит на /login"
  </acceptance_criteria>
  <done>API ключи добавлены, приложение запускается</done>
</task>

<task type="checkpoint:manual">
  <name>Task 3: [CHECKPOINT] Создать тестового пользователя и проверить вход</name>
  <files></files>
  <read_first></read_first>
  <action>
ИНСТРУКЦИЯ ДЛЯ ПОЛЬЗОВАТЕЛЯ:

1. В Supabase Dashboard → Authentication → Users → Add user:
   - Email: admin@test.com
   - Password: Test123456!
   - Нажать "Create user"

2. В Supabase Dashboard → SQL Editor выполнить запрос для создания профиля:
```sql
INSERT INTO profiles (id, email, display_name, role)
SELECT id, email, 'Администратор', 'admin'
FROM auth.users
WHERE email = 'admin@test.com'
ON CONFLICT (id) DO NOTHING;
```

3. Перейти на http://localhost:3000/login
4. Войти с email: admin@test.com, пароль: Test123456!
5. Проверить:
   - ✓ Успешный вход → редирект на /
   - ✓ Обновить страницу → остаётся авторизованным
   - ✓ http://localhost:3000/login → редирект на /
   - ✓ http://localhost:3000/admin → редирект на / (если нет admin роли — проверить)

Сообщите о результатах.
  </action>
  <verify>echo "Manual verification by user"</verify>
  <acceptance_criteria>
    - "Вход работает — редирект на / после успешного login"
    - "Сессия сохраняется после F5"
    - "Авторизованный пользователь на /login → редирект на /"
  </acceptance_criteria>
  <done>Phase 2 подтверждена: аутентификация работает</done>
</task>

</tasks>
