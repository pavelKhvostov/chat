---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/actions/auth.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/layout.tsx
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-04", "AUTH-05"]
must_haves:
  truths:
    - "Server Action signIn принимает email+password, вызывает supabase.auth.signInWithPassword"
    - "Server Action signOut вызывает supabase.auth.signOut и редиректит на /login"
    - "Страница /login рендерится без ошибок с формой email+password"
    - "Успешный вход перенаправляет на /"
    - "Обработка ошибок: неверный пароль показывает сообщение пользователю"
  artifacts:
    - src/lib/actions/auth.ts
    - src/app/(auth)/login/page.tsx
    - src/app/(auth)/layout.tsx
  key_links:
    - "login/page.tsx вызывает signIn action через form action"
    - "signIn использует createClient из server.ts"
---

<objective>
Создать Server Actions для аутентификации и страницу входа /login.

Purpose: Пользователь должен войти в систему чтобы использовать чат. Страница /login — точка входа для всех.
Output: auth.ts actions, страница /login с формой, layout для auth-группы.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@src/lib/supabase/server.ts
@src/lib/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions для аутентификации</name>
  <files>src/lib/actions/auth.ts</files>
  <read_first>src/lib/supabase/server.ts</read_first>
  <action>
Создать src/lib/actions/auth.ts:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function signIn(formData: FormData) {
  const supabase = await createClient()

  const email = formData.get('email') as string
  const password = formData.get('password') as string

  const { error } = await supabase.auth.signInWithPassword({
    email,
    password,
  })

  if (error) {
    return { error: error.message }
  }

  revalidatePath('/', 'layout')
  redirect('/')
}

export async function signOut() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  revalidatePath('/', 'layout')
  redirect('/login')
}

export async function getUser() {
  const supabase = await createClient()
  const { data: { user }, error } = await supabase.auth.getUser()
  if (error || !user) return null
  return user
}

export async function getProfile() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return null

  const { data: profile } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.id)
    .single()

  return profile
}
```
  </action>
  <verify>grep -l "'use server'" src/lib/actions/auth.ts</verify>
  <acceptance_criteria>
    - "src/lib/actions/auth.ts существует"
    - "Файл начинается с 'use server'"
    - "Файл содержит функцию signIn"
    - "Файл содержит функцию signOut"
    - "signIn возвращает { error } при ошибке (не throws)"
  </acceptance_criteria>
  <done>Auth Server Actions созданы</done>
</task>

<task type="auto">
  <name>Task 2: Layout для auth-группы</name>
  <files>src/app/(auth)/layout.tsx</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>
Создать директорию src/app/(auth)/ и файл src/app/(auth)/layout.tsx:

```typescript
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen bg-[#0f0f1a] flex items-center justify-center">
      {children}
    </div>
  )
}
```

Также обновить src/app/layout.tsx — добавить шрифты Inter + JetBrains Mono согласно CLAUDE.md, установить тёмный фон:

```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin', 'cyrillic'],
  variable: '--font-inter',
})

export const metadata: Metadata = {
  title: 'IntraChat',
  description: 'Корпоративный мессенджер',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="ru" className={`${inter.variable}`}>
      <body className="bg-[#0f0f1a] text-white antialiased">
        {children}
      </body>
    </html>
  )
}
```

Для globals.css убедиться что нет светлого фона по умолчанию — добавить:
```css
:root {
  --background: #0f0f1a;
  --foreground: #ffffff;
}
body {
  background: var(--background);
  color: var(--foreground);
}
```
  </action>
  <verify>ls src/app/(auth)/layout.tsx</verify>
  <acceptance_criteria>
    - "src/app/(auth)/layout.tsx существует"
    - "layout.tsx содержит 'bg-[#0f0f1a]'"
    - "src/app/layout.tsx обновлён с Inter шрифтом"
  </acceptance_criteria>
  <done>Auth layout и root layout настроены</done>
</task>

<task type="auto">
  <name>Task 3: Страница /login</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <read_first>src/lib/actions/auth.ts, src/app/(auth)/layout.tsx</read_first>
  <action>
Создать src/app/(auth)/login/page.tsx — форма входа, строгий дизайн согласно CLAUDE.md (тёмная тема, акцент индиго):

```typescript
'use client'

import { useState } from 'react'
import { signIn } from '@/lib/actions/auth'

export default function LoginPage() {
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(formData: FormData) {
    setLoading(true)
    setError(null)
    const result = await signIn(formData)
    if (result?.error) {
      setError('Неверный email или пароль')
      setLoading(false)
    }
  }

  return (
    <div className="w-full max-w-sm px-6">
      {/* Логотип */}
      <div className="mb-8 text-center">
        <h1 className="text-2xl font-semibold tracking-tight text-white">
          IntraChat
        </h1>
        <p className="mt-1 text-sm text-white/40">
          Корпоративный мессенджер
        </p>
      </div>

      {/* Форма */}
      <form action={handleSubmit} className="space-y-4">
        <div>
          <label
            htmlFor="email"
            className="block text-xs font-medium text-white/60 mb-1.5 uppercase tracking-wide"
          >
            Email
          </label>
          <input
            id="email"
            name="email"
            type="email"
            required
            autoComplete="email"
            className="w-full rounded-lg bg-white/5 border border-white/10 px-3.5 py-2.5 text-sm text-white placeholder-white/20 outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-colors"
            placeholder="you@company.com"
          />
        </div>

        <div>
          <label
            htmlFor="password"
            className="block text-xs font-medium text-white/60 mb-1.5 uppercase tracking-wide"
          >
            Пароль
          </label>
          <input
            id="password"
            name="password"
            type="password"
            required
            autoComplete="current-password"
            className="w-full rounded-lg bg-white/5 border border-white/10 px-3.5 py-2.5 text-sm text-white placeholder-white/20 outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-colors"
            placeholder="••••••••"
          />
        </div>

        {error && (
          <p className="text-sm text-red-400 text-center">{error}</p>
        )}

        <button
          type="submit"
          disabled={loading}
          className="w-full rounded-lg bg-indigo-600 hover:bg-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed px-4 py-2.5 text-sm font-medium text-white transition-colors"
        >
          {loading ? 'Вход...' : 'Войти'}
        </button>
      </form>
    </div>
  )
}
```
  </action>
  <verify>ls src/app/(auth)/login/page.tsx</verify>
  <acceptance_criteria>
    - "src/app/(auth)/login/page.tsx существует"
    - "Файл содержит 'use client'"
    - "Файл содержит input[name='email'] и input[name='password']"
    - "Файл импортирует signIn из '@/lib/actions/auth'"
    - "Есть обработка error state"
  </acceptance_criteria>
  <done>Страница /login создана с формой и обработкой ошибок</done>
</task>

<task type="auto">
  <name>Task 4: Заглушка главной страницы и route группы</name>
  <files>src/app/(main)/layout.tsx, src/app/(main)/page.tsx</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Создать заглушки для main-группы (полноценный layout будет в Phase 3):

src/app/(main)/layout.tsx:
```typescript
import { redirect } from 'next/navigation'
import { getUser } from '@/lib/actions/auth'

export default async function MainLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const user = await getUser()
  if (!user) redirect('/login')

  return (
    <div className="flex h-screen bg-[#0f0f1a] text-white">
      {children}
    </div>
  )
}
```

src/app/(main)/page.tsx:
```typescript
export default function HomePage() {
  return (
    <div className="flex items-center justify-center flex-1">
      <p className="text-white/40 text-sm">Выберите чат в левой панели</p>
    </div>
  )
}
```
  </action>
  <verify>ls src/app/(main)/layout.tsx && ls src/app/(main)/page.tsx</verify>
  <acceptance_criteria>
    - "src/app/(main)/layout.tsx существует с getUser() проверкой"
    - "src/app/(main)/page.tsx существует"
  </acceptance_criteria>
  <done>Заглушки main layout и homepage созданы</done>
</task>

<task type="auto">
  <name>Task 5: Проверка TypeScript сборки</name>
  <files></files>
  <read_first></read_first>
  <action>
Запустить TypeScript проверку:

```bash
npx tsc --noEmit 2>&1 | head -30
```

Исправить любые ошибки типизации. Частые проблемы:
- Если cookies() не async в Next.js 14 — проверить версию и убрать await если нужно
- Если нет @types/react — установить: `npm install -D @types/react @types/react-dom`
  </action>
  <verify>npx tsc --noEmit 2>&1; echo "Exit: $?"</verify>
  <acceptance_criteria>
    - "npx tsc --noEmit завершается с exit code 0"
  </acceptance_criteria>
  <done>TypeScript ошибок нет</done>
</task>

</tasks>
