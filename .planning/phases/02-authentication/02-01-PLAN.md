---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/middleware.ts
autonomous: true
requirements: ["AUTH-03", "AUTH-04", "AUTH-05"]
must_haves:
  truths:
    - "@supabase/ssr и @supabase/supabase-js установлены"
    - "src/lib/supabase/client.ts экспортирует createBrowserClient"
    - "src/lib/supabase/server.ts экспортирует createServerClient для Server Components и Actions"
    - "src/middleware.ts обновляет сессию и редиректит неавторизованных на /login"
    - "middleware защищает /admin — редирект если role != admin"
  artifacts:
    - src/lib/supabase/client.ts
    - src/lib/supabase/server.ts
    - src/middleware.ts
  key_links:
    - "server.ts использует cookies() из next/headers"
    - "middleware.ts использует createServerClient из @supabase/ssr"
---

<objective>
Установить Supabase SDK, создать клиентские хелперы и middleware для управления сессией.

Purpose: Основа аутентификации — все Server Actions и Server Components будут использовать createServerClient из этого модуля.
Output: supabase/client.ts, supabase/server.ts, middleware.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@src/lib/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установка Supabase пакетов</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Установить необходимые пакеты:

```bash
npm install @supabase/supabase-js @supabase/ssr
```

Затем создать файл .env.local с переменными (если не существует):
```
NEXT_PUBLIC_SUPABASE_URL=https://cuuoyhikjawiyzgbrnku.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

Примечание: ключи (ANON_KEY, SERVICE_ROLE_KEY) пользователь заполнит из Supabase Dashboard → Settings → API. Создать файл с пустыми значениями и TODO-комментарием.
  </action>
  <verify>node -e "require('@supabase/ssr')" && echo "OK"</verify>
  <acceptance_criteria>
    - "package.json содержит @supabase/ssr"
    - "package.json содержит @supabase/supabase-js"
    - ".env.local существует с NEXT_PUBLIC_SUPABASE_URL"
  </acceptance_criteria>
  <done>Supabase SDK установлен</done>
</task>

<task type="auto">
  <name>Task 2: Browser client (client.ts)</name>
  <files>src/lib/supabase/client.ts</files>
  <read_first>src/lib/types/database.types.ts</read_first>
  <action>
Создать src/lib/supabase/client.ts:

```typescript
import { createBrowserClient } from '@supabase/ssr'
import { type Database } from '@/lib/types/database.types'

export function createClient() {
  return createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

Использование: в Client Components ('use client') для чтения данных и подписок Realtime.
  </action>
  <verify>grep -l "createBrowserClient" src/lib/supabase/client.ts</verify>
  <acceptance_criteria>
    - "src/lib/supabase/client.ts существует"
    - "Файл содержит 'createBrowserClient'"
    - "Файл содержит 'Database' generic"
  </acceptance_criteria>
  <done>Browser client создан</done>
</task>

<task type="auto">
  <name>Task 3: Server client (server.ts)</name>
  <files>src/lib/supabase/server.ts</files>
  <read_first>src/lib/types/database.types.ts</read_first>
  <action>
Создать src/lib/supabase/server.ts:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { type Database } from '@/lib/types/database.types'

export async function createClient() {
  const cookieStore = await cookies()

  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Вызывается из Server Component — cookies устанавливаются в middleware
          }
        },
      },
    }
  )
}
```

Использование: в Server Actions (`'use server'`) и Server Components для чтения данных с авторизацией.
  </action>
  <verify>grep -l "createServerClient" src/lib/supabase/server.ts</verify>
  <acceptance_criteria>
    - "src/lib/supabase/server.ts существует"
    - "Файл содержит 'createServerClient'"
    - "Файл содержит 'cookies()' из next/headers"
    - "Файл содержит 'Database' generic"
  </acceptance_criteria>
  <done>Server client создан</done>
</task>

<task type="auto">
  <name>Task 4: Middleware для управления сессией</name>
  <files>src/middleware.ts</files>
  <read_first>src/lib/supabase/server.ts</read_first>
  <action>
Создать src/middleware.ts (в корне src/, рядом с app/):

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({
    request,
  })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({
            request,
          })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  // Обновляем сессию (ВАЖНО: не удалять)
  const {
    data: { user },
  } = await supabase.auth.getUser()

  const { pathname } = request.nextUrl

  // Неавторизованный → /login
  if (!user && pathname !== '/login') {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  // Авторизованный на /login → перенаправляем в чат
  if (user && pathname === '/login') {
    const url = request.nextUrl.clone()
    url.pathname = '/'
    return NextResponse.redirect(url)
  }

  // /admin — только для role=admin
  if (pathname.startsWith('/admin') && user) {
    const { data: profile } = await supabase
      .from('profiles')
      .select('role')
      .eq('id', user.id)
      .single()

    if (profile?.role !== 'admin') {
      const url = request.nextUrl.clone()
      url.pathname = '/'
      return NextResponse.redirect(url)
    }
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```
  </action>
  <verify>grep -l "supabase.auth.getUser" src/middleware.ts</verify>
  <acceptance_criteria>
    - "src/middleware.ts существует"
    - "Файл содержит 'supabase.auth.getUser'"
    - "Файл содержит '/login' redirect логику"
    - "Файл содержит '/admin' защиту с проверкой role"
    - "Файл содержит 'export const config' с matcher"
  </acceptance_criteria>
  <done>Middleware создан с защитой роутов и проверкой admin роли</done>
</task>

</tasks>
